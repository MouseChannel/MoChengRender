#include "Description.hpp"
#include <vector>

namespace SoftRender {
class Buffer;

class Pipeline {
public:
    Pipeline();
    ~Pipeline();

private:
    // std::vector<std::pair<std::unique_ptr<Buffer>, VertexDes>> vertex_data;
};

}